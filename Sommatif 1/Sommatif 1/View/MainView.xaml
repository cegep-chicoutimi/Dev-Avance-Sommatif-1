<Window x:Class="Sommatif_1.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sommatif_1.View"
        xmlns:vm="clr-namespace:Sommatif_1.View_Model"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:LoginViewmodel}">
            <local:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:InformationViewModel}">
            <local:InformationView/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ContentControl  Content="{Binding ViewModelAfctif}"/>
    </Grid>
</Window>
