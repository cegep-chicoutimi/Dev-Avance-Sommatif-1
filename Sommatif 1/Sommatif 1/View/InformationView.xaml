<UserControl x:Class="Sommatif_1.View.InformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sommatif_1.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView Grid.Row="0" ItemsSource="{Binding Utilisateurs}" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="UserName" DisplayMemberBinding="{Binding User}" Width="auto"/>
                    <GridViewColumn Header="Nom" DisplayMemberBinding="{Binding Password}" Width="auto"/>
                    <GridViewColumn Header="Prénom" DisplayMemberBinding="{Binding DateLog}" Width="auto"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
