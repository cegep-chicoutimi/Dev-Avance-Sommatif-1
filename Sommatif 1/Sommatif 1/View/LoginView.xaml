<UserControl x:Class="Sommatif_1.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sommatif_1.View"
             mc:Ignorable="d" 
             Background="Beige"
             d:DesignHeight="450" d:DesignWidth="800">
    <WrapPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal" Margin="20" >
            <Label Content="User" Margin="10"/>
            <TextBox Width="150" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="25"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="20">
            <Label Content="Pass" Margin="10"/>
            <TextBox Width="150" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Height="25"/>
            <TextBlock Text="{Binding DataContext.MessageErreur,
                          RelativeSource={RelativeSource AncestorType=Window}}"
           Foreground="Red"
           Margin="10"/>
        </StackPanel>
        <Button Content="Login" Command="{Binding DataContext.VerrificationCommande,
                          RelativeSource={RelativeSource AncestorType=Window}}"/>
    </WrapPanel>
</UserControl>
